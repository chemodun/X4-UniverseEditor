<Window x:Class="ChemGateBuilder.GalaxyMapWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Galaxy Map" Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        SizeChanged="GalaxyMapWindow_SizeChanged"
        Icon="{StaticResource BuilderIcon}"
        PreviewMouseWheel="Window_PreviewMouseWheel">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Slider x:Name="HexagonWidthSlider"
                Grid.Row="0"
                Minimum="{Binding HexagonWidthMinimal}"
                Maximum="{Binding HexagonWidthMaximal}"
                Value="{Binding HexagonWidth}"
                TickFrequency="10" IsSnapToTickEnabled="True"
                TickPlacement="BottomRight"/>
        <ScrollViewer x:Name="GalaxyScrollViewer"
                        Grid.Row="1"
                        HorizontalScrollBarVisibility="Auto"
                        VerticalScrollBarVisibility="Auto"
                        Background="LightGray"
                        ScrollChanged="GalaxyScrollViewer_ScrollChanged">
            <Canvas x:Name="GalaxyCanvas"
                Background="LightGray"
                />
        </ScrollViewer>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Vertical">
            <GroupBox Header="DLC's" Margin="5">
                <ItemsControl ItemsSource="{Binding EnabledDLCs}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Name}" IsChecked="{Binding IsChecked}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </GroupBox>
            <GroupBox Header="Mods" Margin="5">
                <ItemsControl ItemsSource="{Binding EnabledMods}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Name}" IsChecked="{Binding IsChecked}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </GroupBox>
        </StackPanel>
    </Grid>
</Window>